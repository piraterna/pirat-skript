# Built in variables:
#   - argv (list)
#   - _version
#   - _host


# Invoke built-in python functions:
invoke print("Ahoy!")

# Invoke system commands:
invoke $echo("Ahoy to you! Here is my files:")
invoke $ls

# Define variables:
let name = Kevin

# Use variables in args:
invoke print("Ahoy {name}")

# Use system ENV variables in args:
invoke print("User: {$USER}")